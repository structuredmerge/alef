{% if doc_comment %}{{ doc_comment }}{% else %}    # @return [{{ sorbet_t }}]
{% endif %}    sig { returns({{ sorbet_t }}) }
    def {{ attr_name }} = Data.instance_method(:to_h).bind_call(self).fetch(:{{ attr_name }})
